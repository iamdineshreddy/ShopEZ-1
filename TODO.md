# TODO

- [x] Add root `package.json` so tooling can find it at `d:/Shopez/package.json`.
- [x] Verify root scripts (`npm install`, `npm run dev` and workspace starts).
- [x] Added server `.env` template (MONGO_URI + JWT_SECRET) so login/register can work when MongoDB is running.
- [ ] Provide test credentials and ensure database seeding is possible.

